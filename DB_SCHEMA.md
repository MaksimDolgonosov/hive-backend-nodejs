# DB_SCHEMA.md — устарел

Этот документ описывал схему PostgreSQL/PostGIS для старой версии стека
(NestJS + Prisma). Проект перешёл на Node.js + Express + MongoDB.

Актуальная схема данных (Mongoose-модели, геоиндексы, TTL) теперь находится
в `BACKEND_ARCHITECTURE.md`, раздел 4 «Модели данных (Mongoose)» и далее.

Этот файл оставлен только для истории решения — самостоятельной ценности
не несёт, можно удалить.
